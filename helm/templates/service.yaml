apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name}}-svc"
spec:
  ports:
  - port: {{ .Values.service.port }}
    protocol: TCP
    targetPort: 80
  selector:
    env: {{ .Release.Name}}
    project: {{ .Release.Name}}
  type: {{ .Values.service.type }}
